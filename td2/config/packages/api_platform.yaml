api_platform:
  title: 'API title'
  description: 'API description'
  version: '1.0.0'
  # Set this to false if you want Webby to disappear.
  show_webby: false
  swagger:
    versions: [3]
    api_keys:
      apiKey:
        name: Authorization
        type: header